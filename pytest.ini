[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --verbose --cov=ai_platform_trainer --cov-report=term --cov-report=html
markers =
    unit: unit tests
    integration: integration tests
    slow: tests that take a long time to run
    model: tests related to neural network models
    gameplay: tests related to game mechanics
    ai: tests related to AI controllers
